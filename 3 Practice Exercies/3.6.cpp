#include <iostream>
using namespace std;

int main() {
    
    int x,y;
    cout << "Insert two integer x and y: ";
    cin >> x >> y;

    int p = x*y;
    int s = x+y;

    // q = s2+p(sx)*(p+y) 
    // no la vay do

    return 0;
}